# 頭城地方記憶典藏事件資料模型 v1.6

每一筆事件都保留原始敘述，同時增加固定研究欄位。空白代表「尚無資料」，不以推測值補入。

| 欄位 | 說明 |
|---|---|
| `id` | 穩定且不重複的事件識別碼 |
| `startDate` | 事件開始日期，可使用 `YYYY`、`YYYY-MM` 或 `YYYY-MM-DD` |
| `endDate` | 事件結束日期；未知時留空 |
| `title` | 事件標題 |
| `event` | 原始事件摘要 |
| `locations` | 地點名稱陣列 |
| `villages` | 行政里陣列 |
| `people` | 關聯人物陣列 |
| `organizations` | 關聯機構或店家陣列 |
| `primaryCategory` | 十大主題分類中的一項 |
| `tags` | 次標籤陣列 |
| `sources` | 多筆資料來源陣列，同一事件不因來源不同而重複建檔 |
| `review` | 可信度、校訂狀態、備註與最後更新日期 |

## `sources` 欄位

- `name`：來源名稱
- `url`：原始網址
- `type`：官方、地方出版、新聞、地方生活、民間傳說或延伸參考
- `sourceDate`：來源發布日期；未知時留空
- `note`：來源差異或閱讀提醒

## `review` 欄位

- `status`：原有整理狀態
- `confidence`：高、中、待交叉查證或待查證
- `notes`：人工校訂備註
- `lastUpdated`：最後更新日期

## 編輯原則

1. 不把只有年份的事件改成1月1日。
2. 疑似重複只列入校訂報告，未經人工確認不刪除。
3. 同一事件有不同來源時，在 `sources` 中並列來源與差異。
4. 官方、出版、新聞與居民記憶可並列，但必須保留來源類型及校訂狀態。
