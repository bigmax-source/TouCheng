# 頭城地方記憶典藏資料模型 v1.72

每一筆事件都保留原始敘述，同時增加固定研究欄位。空白代表「尚無資料」，不以推測值補入。

| 欄位 | 說明 |
|---|---|
| `id` | 穩定且不重複的事件識別碼；公開顯示為 `TC-H-{id}` 典藏編號 |
| `startDate` | 事件開始日期，可使用 `YYYY`、`YYYY-MM` 或 `YYYY-MM-DD` |
| `endDate` | 事件結束日期；未知時留空 |
| `title` | 事件標題 |
| `event` | 原始事件摘要 |
| `locations` | 地點名稱陣列 |
| `villages` | 行政里陣列 |
| `people` | 關聯人物陣列 |
| `organizations` | 關聯機構或店家陣列 |
| `primaryCategory` | 十大主題分類中的一項 |
| `tags` | 次標籤陣列 |
| `sources` | 多筆資料來源陣列，同一事件不因來源不同而重複建檔 |
| `review` | 可信度、校訂狀態、備註與最後更新日期 |

## `sources` 欄位

- `name`：來源名稱
- `url`：原始網址
- `type`：官方、地方出版、新聞、地方生活、民間傳說或延伸參考
- `sourceDate`：來源發布日期；未知時留空
- `note`：來源差異或閱讀提醒

## `review` 欄位

- `status`：原有整理狀態
- `confidence`：高、中、待交叉查證或待查證
- `notes`：人工校訂備註
- `lastUpdated`：最後更新日期

## 編輯原則

1. 不把只有年份的事件改成1月1日。
2. 疑似重複只列入校訂報告，未經人工確認不刪除。
3. 同一事件有不同來源時，在 `sources` 中並列來源與差異。
4. 官方、出版、新聞與居民記憶可並列，但必須保留來源類型及校訂狀態。

## 版本完整性

建置時會把最終事件依 `id` 排序，以 UTF-8、鍵名排序且無多餘空白的標準化 JSON 計算 SHA-256。結果輸出至 `assets/data/data-integrity.json` 與 `.js`，供校訂頁公開核對。典藏編號沿用既有穩定 ID，不因排序或分檔變動而重新編號。

## 地點資料模型 v1.7

地點原始資料位於 `tools/places-source.json`，由 `tools/build-places.mjs` 產生網站使用的 `places-data.js` 與事件反向索引。

| 欄位 | 說明 |
|---|---|
| `id` | 穩定且不重複的地點識別碼 |
| `name` | 地點正式顯示名稱 |
| `category` | 四個地圖圖層之一 |
| `lat`、`lng` | 地點代表座標；歷史聚落不以單一座標取代其範圍 |
| `address` | 現行地址或位置描述 |
| `summary` | 地方閱讀簡介 |
| `matchTerms` | 用來建立事件關聯的受控名稱 |
| `historyQuery` | 前往完整時間軸時使用的搜尋詞 |
| `sourceUrl` | 地址、座標或簡介的可追溯資料來源 |
| `peopleUrl` | 可選；連往人物與地方專題 |

產生後的公開資料會自動增加 `eventIds` 與精簡的 `relatedEvents`。同一事件可連到多個地點，反向索引則讓時間軸顯示「在地方地圖查看」。
